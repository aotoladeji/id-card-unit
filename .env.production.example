# Frontend Production Environment (Vercel)
# Set these in: Vercel Project Settings > Environment Variables

# Not required when backend is deployed on the same Vercel project.
# The frontend defaults to /api which Vercel routes to the backend serverless function.
# VITE_API_BASE_URL=/api
