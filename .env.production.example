# Frontend Production Environment (Render Static Site)
# Set this in Render Static Site > Environment.

# Render static sites do not proxy /api by default, so use the backend's full URL.
VITE_API_BASE_URL=https://<YOUR_RENDER_BACKEND>.onrender.com/api
